@model IEnumerable<PaulSchool.Models.CourseTemplates>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Title
        </th>
        <th>
            Credits
        </th>
        <th>
            Elective
        </th>
        <th>
            AttendingDays
        </th>
        <th>
            AttendanceCap
        </th>
        <th>
            Location
        </th>
        <th>
            Parish
        </th>
        <th>
            Description
        </th>
        <th>
            Cost
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Credits)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Elective)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AttendingDays)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AttendanceCap)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Parish)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseTemplatesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseTemplatesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseTemplatesID })
        </td>
    </tr>
}

</table>
